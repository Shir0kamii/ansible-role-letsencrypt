server {

        server_name _;

        listen 80 default_server;
        listen [::]:80 default_server;

        root /var/www/html;

        location / {
            try_files $uri $uri/ =404;
        }

        location /.well-known/ {
                root {{ letsencrypt_webroot }};
        }

}
